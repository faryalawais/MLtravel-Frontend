# CP-002 Visual Fidelity Review

> Generated by `visual-regression` on 2026-07-06.  
> **Purpose:** Human aid for slice approval — not an automated pass/fail vs Figma.  
> **Contract:** `features/CP-002/contract.md` · tolerance `0.02` for ML Travel chrome only.

## Automation status (Job 1 + Job 3)

| Artifact | Status |
|----------|--------|
| `tests/visual/cp-002.spec.ts` | **Baseline established** — 7 golden-master snapshots (desktop + mobile slices, full page) |
| `tests/visual/cp-002-dimensions.spec.ts` | **Pass** — typography + embed/fallback dimensions @ 1440px |
| `npm run test:visual` (CP-002) | **11/11 pass** on this machine |
| Human baseline approval | **Pending** — first-run baselines require human sign-off |

**Note:** Golden-master baselines catch drift between runs; they do not prove Figma match. Use this report for Figma fidelity.

## Screenshot pairs

| Slice | Figma reference | App capture (2026-07-06) |
|-------|-----------------|----------------------------|
| Hero | `features/CP-002/figma/reference-contacthero.png` | `reports/cp-002-screenshots/contact-hero-desktop.png` |
| Hero mobile | — (no mobile Figma) | `reports/cp-002-screenshots/contact-hero-mobile.png` |
| Embed container | `features/CP-002/figma/reference-calendlyembedslot.png` | `reports/cp-002-screenshots/contact-embed-desktop.png` (iframe masked) |
| Embed mobile | — | `reports/cp-002-screenshots/contact-embed-mobile.png` (iframe masked) |
| Email fallback | `features/CP-002/figma/reference-emailfallback.png` | `reports/cp-002-screenshots/contact-fallback-desktop.png` |
| Fallback mobile | — | `reports/cp-002-screenshots/contact-fallback-mobile.png` |
| Full page stack | `5185:4332` (chrome only) | `reports/cp-002-screenshots/contact-page-desktop.png` (iframe masked) |

---

## §2 Anatomy — section-by-section verdicts

### Page shell (`5185:4332`)

| Element | Verdict | Notes |
|---------|---------|-------|
| Navbar (`component.navbar`, LP-001 reuse) | **Present** | Rendered via `app/layout.tsx`; outside `screen.contact.page` screenshot scope — verify in browser @ `/contact` |
| Main column (hero → embed → fallback) | **Present** | Vertical stack, centered; `contact-page-desktop.png` shows correct order |
| Footer (`component.footer`, LP-001 reuse) | **Present** | Same as navbar — in layout, below main content |

### GH#14 — Contact hero (`5198:3440`)

| Element | Verdict | Notes |
|---------|---------|-------|
| `component.contact.hero` root frame | **Present** | Centered column, max-width 755px |
| Hero inner stack (`5198:3443`) | **Present** | Column layout with `space.sm` gap |
| Headline — "Get a live demo of your platform" (`5198:3444`) | **Present** | Copy matches Figma; `typography.display.desktop.lg` (48px / 700) asserted |
| Subhead (`5198:3445`) | **Present** | Copy matches Figma; `typography.body.desktop.md` (16px / 400) asserted |
| Text colour token | **Present** | `color-text-primary` per design system (`--color-text-primary`) |
| Mobile layout (393px) | **Present** | Headline wraps naturally; centered stack; no mobile Figma frame to pixel-match |

### GH#15 — Calendly embed slot (`5186:4368`)

| Element | Verdict | Notes |
|---------|---------|-------|
| `component.contact.embed` container | **Present** | 868×550 ±4px @ 1440px (dimension test pass) |
| Embed skeleton (`component.contact.embedSkeleton`) | **Present** | Implemented; not captured in golden-master (loaded-state screenshot) |
| Calendly iframe | **Present** | Native `<iframe>` with `NEXT_PUBLIC_CALENDLY_URL`; masked in visual tests per §11 |
| Account-details-summary mock (`5186:4369`) | **N/A** | Contract: Calendly mock — **not rebuilt**; lives inside iframe at runtime |
| Calendar section mock (`5186:4390`) | **N/A** | Same — Calendly-owned UI inside iframe |
| Embed fallback (`component.contact.embedFallback`) | **Present** | Implemented; not visible in default env (Calendly URL configured) — verify via GH#15 skipped scenario |
| Mobile embed (fluid width) | **Present** | Full-width container with horizontal page padding @ 393px |

### GH#16 — Email fallback (`5198:3429`)

| Element | Verdict | Notes |
|---------|---------|-------|
| `component.contact.fallback` root frame | **Present** | Centered column, max-width 560px |
| Fallback copy stack (`5198:3430`) | **Present** | Column with `space.sm` gap |
| Heading — "Not ready to book a slot?" (`5198:3431`) | **Present** | Copy matches; `typography.heading.desktop.h2` (26px / 700) asserted |
| Body copy (`5198:3432`) | **Present** | Copy matches; `typography.body.desktop.xs` (14px / 400); muted text colour |
| Email CTA (`5198:3433`, Button/Secondary2) | **Present** | White/navy secondary button; border `color.border.brand-navy`; radius `radius.6`; pad 28×12 |
| CTA label — `hello@maqsoodtravel.com` | **Present** | Matches Figma visual reference and `CONTACT_FALLBACK_EMAIL` constant |
| CTA typography token | **Present** | `text-label-desktop-lg` (16px / 600) per Figma cache `5198:3433` |
| Icon/Button plane arrow (`I5198:3433;2780:1463`) | **Present** | `icon-button-arrow.svg` 16×16 right of label — confirm in browser (small asset) |
| CTA hover / focused states | **Not reviewed** | Tokens wired in `CONTACT_EMAIL_CTA_CLASS_NAME`; human spot-check at Step 7 |
| Mobile fallback (393px) | **Present** | Stacked copy + CTA; remains below embed |

### Reference frames — excluded from React (contract §2)

| Frame | Verdict |
|-------|---------|
| Choose hour (`5186:4533`) | **N/A** — embed sizing reference only |
| Details (`5186:4857`) | **N/A** — Calendly form inside iframe |
| Summary (`5198:4102`) | **N/A** — Calendly admin / confirmation copy (AC-12 ops) |

---

## Motion (Job 4)

CP-002 has **no animation twins** (`validate:motion-chains` skip). No motion subsections required.

| Section | Verdict |
|---------|---------|
| All CP-002 chrome | **Not reviewed** — static only; no hover chains in scope |

---

## Summary

| Category | Count |
|----------|-------|
| **Present** | All in-scope ML Travel chrome elements |
| **Differs** | **0** |
| **Missing** | **0** |
| **N/A (iframe / ops)** | Calendly interior UI; reference frames 4533/4857/4102 |

### Approval gate

- **Golden-master:** Baselines created — **pending human approval** of initial renders.
- **Figma fidelity:** No **Missing** or **Differs** findings. Human should confirm hero/subhead/CTA visually against Figma refs before APPROVE.
- **Recommended human checks:**
  1. Open `/contact` @ 1440px — compare hero, fallback, navbar/footer to Figma `5185:4332`.
  2. Confirm email CTA arrow icon visible beside `hello@maqsoodtravel.com`.
  3. Hover/focus email CTA — secondary hover/focus tokens.
  4. Unset `NEXT_PUBLIC_CALENDLY_URL` locally — verify embed fallback message + external link.
  5. Commit baselines under `tests/visual/__screenshots__/visual/cp-002.spec.ts/` after approval.
