# Design Contract — CP-002 Contact Us (Calendly demo booking)

> Generated by `design-contract` on 2026-07-06.  
> **Scope:** Contact page chrome at `/contact` — hero `5198:3440`, Calendly embed slot `5186:4368`, email fallback `5198:3429`. Shared Navbar + Footer from LP-001 (not re-implemented).  
> Source: `features/CP-002/figma/spec.json` · file `h6BqI1ZRMSJxR7jESNF0Ep` · canonical frame `5185:4332`

## 1. Routes

| Path | Purpose |
|------|---------|
| `/contact` | Contact / demo booking — `screen.contact.page` |

**Page flow (desktop 1440px):** LP-001 `component.navbar` → `component.contact.hero` → `component.contact.embed` (Calendly iframe) → `component.contact.fallback` → LP-001 `component.footer`.

## 1a. UI registry entries

> Gherkin uses 2-segment paths (`screen.contact`, `component.contact.hero`). Canonical registry paths resolve via `tokens/ui-registry.json` `$metadata.gherkinAliases`.

### Screens

| Path | Description |
|------|-------------|
| `screen.contact` | Contact page at `/contact` (registry: `screen.contact.page`) |

### Components

| Path | Description | Parent screen | States |
|------|-------------|---------------|--------|
| `component.navbar` | Shared site navbar from LP-001 | `screen.contact` | `default`, `hover` |
| `component.footer` | Shared site footer from LP-001 | `screen.contact` | `default` |
| `component.contact.hero` | Hero headline + subhead | `screen.contact` | `default` |
| `component.contact.embed` | Calendly embed container (iframe slot) | `screen.contact` | `loading`, `loaded`, `error` |
| `component.contact.embedSkeleton` | Loading skeleton inside embed container | `screen.contact` | `visible`, `hidden` |
| `component.contact.embedFallback` | Fallback when Calendly URL unset or iframe fails | `screen.contact` | `default` |
| `component.contact.fallback` | Email fallback block below embed | `screen.contact` | `default`, `hover`, `focused` |

> **Implementation detail:** Sub-elements (hero heading/subhead, fallback email CTA, etc.) are registered in tokens/ui-registry.json for data-testid and fe-implement. See §2 anatomy.

## 1b. API field bindings

No API-sourced Contact page content in v1. Static copy from constants; email display/href from `NEXT_PUBLIC_CONTACT_EMAIL`. Calendly booking data lives inside the iframe (Calendly origin). LP-001 `GET /api/health` satisfies pipeline BE gate only — not called on page load.

| Field path | JSON Pointer | Displays at | Source |
|------------|--------------|-------------|--------|
| — | — | — | N/A |

**Environment bindings (not `field.*`):**

| Env var | Binds to | Notes |
|---------|----------|-------|
| `NEXT_PUBLIC_CALENDLY_URL` | `component.contact.embed` iframe `src` | e.g. `https://calendly.com/faryal-awais-maqsoodlabs/discovery-call-with-maqsoodtravel` |
| `NEXT_PUBLIC_CONTACT_EMAIL` | `component.contact.fallback` email CTA label + `mailto:` href | Figma shows `hello@maqsoodtravel.com` as visual reference only |

## 2. Component anatomy

> Composition authority: `features/CP-002/figma/layout.json` — vertical stack: `component.contact.hero.root` → `component.contact.embed.root` → `component.contact.fallback.root`.  
> Registry slugs use `.root` suffix; Gherkin asserts `component.contact.hero` / `component.contact.embed` / `component.contact.fallback`.

### Page shell (`5185:4332` meta — no full-page cache)

```
Contact page  (figmaNode 5185:4332, 1440×1020)  [screen.contact.page via layout]
├─ Navbar  (LP-001 reuse — nodeId 5164:6559 desktop / 5164:7031 mobile)  [component.navbar]  data-testid
├─ Main column  (centered content stack — hero → embed → fallback)
│  ├─ Contact hero slice  (see below)
│  ├─ Calendly embed slot  (see below)
│  └─ Email fallback slice  (see below)
└─ Footer  (LP-001 reuse — nodeId 5164:6565 desktop / 5164:7038 mobile)  [component.footer]  data-testid
```

### GH#14 — Contact hero (`5198:3440`, 755×92)

```
Frame 2095585128  (nodeId 5198:3440, 755×92)  [component.contact.hero]  data-testid
└─ Frame 2095585124  (nodeId 5198:3441, 755×92)
   └─ Frame 2095585123  (nodeId 5198:3442, 755×92)
      └─ Frame 2095585122  (nodeId 5198:3443, 755×92)
         ├─ Get a live demo of your platform  (nodeId 5198:3444) — headline; typography `typography.display.desktop.lg`
         └─ Pick a time and we'll show you exactly what the switch looks like for your setup.  (nodeId 5198:3445) — subhead; typography `typography.body.desktop.md`
```

### GH#15 — Calendly embed slot (`5186:4368`, 868×550)

```
Choose day  (nodeId 5186:4368, 868×550)  [component.contact.embed]  data-testid
├─ Embed skeleton  (no Figma node — PRD-defined)  [component.contact.embedSkeleton]  data-testid
│  └─ Pulsing placeholder matching 868×550 container — visible until iframe `onLoad`
├─ Calendly iframe  (runtime — replaces Figma mock below)
│  └─ `<iframe src={NEXT_PUBLIC_CALENDLY_URL} title="Schedule a discovery call with MaqsoodTravel" />` — min 868×550 desktop; fluid width mobile
├─ Embed fallback  (no Figma node — PRD-defined)  [component.contact.embedFallback]  data-testid
│  └─ Message + external link to Calendly when URL unset or iframe error
├─ Account-details-summary  (nodeId 5186:4369) — **Calendly mock — NOT rebuilt in React** (lives inside iframe at runtime)
└─ Calendar section  (nodeId 5186:4390) — **Calendly mock — NOT rebuilt in React** (lives inside iframe at runtime)
```

> **Figma fidelity note:** `reference-calendlyembedslot.png` shows the two-column Calendly mock (event summary + date picker). FE implements only the **container** dimensions and centering; Calendly renders all widget UI inside the iframe.

### GH#16 — Email fallback (`5198:3429`, 560×170)

**Variant matrix (cache `5198-3429.json`):**

| nodeId | Component set | State | IconPosition | Notes |
|--------|---------------|-------|--------------|-------|
| `5198:3433` | `Button/Secondary2` | Default | Right | Email CTA — `mailto:` from env |

```
Frame 2095585127  (nodeId 5198:3429, 560×170)  [component.contact.fallback]  data-testid
├─ Frame 2095585126  (nodeId 5198:3430, 560×106)
│  ├─ Not ready to book a slot?  (nodeId 5198:3431) — heading; typography `typography.heading.desktop.h2`
│  └─ Send us a few lines about your agency, which GDS you are on, and what you want to change. We will reply within one business day with a view on what switching would actually look like for you.  (nodeId 5198:3432) — body; typography `typography.body.desktop.xs`
└─ Button/Secondary2  (nodeId 5198:3433, 285×48) — email CTA; label from `NEXT_PUBLIC_CONTACT_EMAIL` (Figma ref: `hello@maqsoodtravel.com`)
   ├─ label  (nodeId I5198:3433;2780:1462) — email address text
   └─ Icon/Button  (nodeId I5198:3433;2780:1463; Icon=Plane Arrow)
      └─ Vector arrow — <Image src="/icons/icon-button-arrow.svg" alt="" width={16} height={16} />
```

### Reference frames — not implemented in React (visual / ops reference only)

**Choose hour** (`5186:4533`) — embed sizing reference; Calendly time-slot step inside iframe.

```
Choose hour  (nodeId 5186:4533)
├─ Navbar  (nodeId 5186:4534) — LP-001 shared chrome
├─ Frame 2095585128  (nodeId 5198:3447) — duplicate hero chrome
├─ Choose day  (nodeId 5186:4541) — Calendly mock (hour picker)
├─ Frame 2095585127  (nodeId 5198:3454) — duplicate fallback chrome
└─ Calendly booking widget embeds here  (nodeId 5198:3487) — designer placeholder; **excluded from implementation**
```

**Details** (`5186:4857`) — embed sizing reference; Calendly details form inside iframe.

```
Details  (nodeId 5186:4857)
├─ Navbar  (nodeId 5186:4858) — LP-001 shared chrome
├─ Frame 2095585128  (nodeId 5198:3476) — duplicate hero chrome
├─ Choose day  (nodeId 5186:4865) — Calendly mock (details form)
├─ Frame 2095585127  (nodeId 5198:3465) — duplicate fallback chrome
└─ Calendly booking widget embeds here  (nodeId 5198:3489) — designer placeholder; **excluded**
```

**Summary** (`5198:4102`) — Calendly admin copy reference (AC-12); confirmation UI inside iframe.

```
Summary  (nodeId 5198:4102)
├─ brand-header  (nodeId 5198:4143) — Calendly confirmation header (ops config)
├─ Frame 91143  (nodeId 5198:4104) — scheduled confirmation copy block
└─ Frame 91142  (nodeId 5198:4111) — event details block
```

## 3. Layout & spacing

| Container | Direction | Gap | Padding | Alignment | Sizing |
|-----------|-----------|-----|---------|-----------|--------|
| Page main column | `column` | — | — | `center` (horizontal) | fluid width; max content ~868px embed |
| `component.contact.hero` (`5198:3440`) | `column` | `space.sm` (8px) | — | `center` | 755×92 content; headline may span wider on desktop |
| Hero inner stack (`5198:3443`) | `column` | `space.sm` (8px) | — | `center` | — |
| `component.contact.embed` (`5186:4368`) | `row` (Figma mock) | — | — | `center` on page | **868×550** min container desktop; fluid mobile |
| `component.contact.fallback` (`5198:3429`) | `column` | `space.md` (16px) | — | `center` | 560×170 |
| Fallback copy stack (`5198:3430`) | `column` | `space.sm` (8px) | — | `center` | — |
| Email CTA (`5198:3433`) | `row` | `space.sm` (8px) | `spacing.28` × `spacing.12` (28×12px) | `center` | 285×48 |

**Vertical rhythm (desktop, from `5185:4332`):** Navbar 64px → hero ~88px offset → embed ~220px → fallback ~810px. Implement with tokenized section gaps; exact page offsets are governed by centered column + natural stack (no raw px in components).

## 4. Tokens per element

| Element | Background | Text | Border | Radius | Spacing | Font |
|---------|-----------|------|--------|--------|---------|------|
| Hero headline (`5198:3444`) | — | `color.text.primary` (`--color-text-primary`) | — | — | — | `typography.display.desktop.lg` |
| Hero subhead (`5198:3445`) | — | `color.text.primary` (`--color-text-primary`) | — | — | — | `typography.body.desktop.md` |
| Fallback heading (`5198:3431`) | — | `color.text.primary` (`--color-text-primary`) | — | — | — | `typography.heading.desktop.h2` |
| Fallback body (`5198:3432`) | — | `color.text.muted` | — | — | — | `typography.body.desktop.xs` |
| Email CTA default (`5198:3433`) | `color.action.secondary.default.background` | `color.action.secondary.default.label` | `color.border.brand-navy` 1px | `radius.6` | pad `spacing.28` / `spacing.12` | `typography.label.desktop.lg` (16/600 per Figma cache) |
| Email CTA hover | `color.action.secondary.hover.background` | `color.action.secondary.hover.label` | `color.border.brand-navy` | `radius.6` | same | same |
| Email CTA focused | `color.action.secondary.focused.background` | `color.action.secondary.focused.label` | `color.action.secondary.focused.border` | `radius.6` | same | same |
| Embed skeleton | `color.surface.muted` | — | — | `radius.6` | — | — |
| Embed fallback text/link | `color.surface.base` | `color.text.default` / `color.action.primary.default.label` | — | — | `space.md` | `typography.body.desktop.md` |

## 5. States & motion

### 5a. Interaction states

| Element | State | Visual change (tokens) |
|---------|-------|---------------------------|
| `component.contact.hero` | default | Static copy — no interaction |
| `component.contact.embed` | loading | Skeleton visible; iframe hidden |
| `component.contact.embed` | loaded | Skeleton hidden; iframe visible |
| `component.contact.embed` | error | `component.contact.embedFallback` visible |
| `component.contact.embedSkeleton` | visible / hidden | Toggled on iframe load lifecycle |
| `component.contact.embedFallback` | default | Message + external Calendly anchor |
| `component.contact.fallback` email CTA | default | `color.action.secondary.default.*` |
| `component.contact.fallback` email CTA | hover | `color.action.secondary.hover.*` |
| `component.contact.fallback` email CTA | focused | `color.action.secondary.focused.*` + `color.action.secondary.focused.border` |
| `component.navbar` / `component.footer` | — | LP-001 contract §5a |

### 5b. Motion-spec (Track B — supplementary)

Not applicable — CP-002 has no animation twins (`validate:motion-chains` skip).

## 6. Responsive

| Breakpoint | What changes |
|------------|--------------|
| Desktop (1440px) | Pixel match Figma `5185:4332` for ML Travel chrome; embed container min 868×550 centered |
| Mobile (393px) | Fluid stacked layout — hero, embed, fallback full width with horizontal padding; no mobile Figma frame; navbar/footer LP-001 mobile variants |
| All | Fallback block remains **below** embed (`component.contact.fallback` below `component.contact.embed`) |

## 7. Accessibility

- **Landmarks:** `<main>` wraps contact content; LP-001 navbar/footer landmarks unchanged.
- **Iframe:** `title="Schedule a discovery call with MaqsoodTravel"` (or equivalent descriptive string).
- **Email CTA:** `<a href="mailto:…">` with visible email label; focus ring via `color.action.secondary.focused.border`.
- **Focus order:** Navbar → hero (static) → embed/fallback link if shown → iframe (tab into Calendly) → email CTA → footer.
- **Contrast:** ML Travel chrome meets WCAG AA via design tokens; Calendly widget contrast is Calendly-owned.
- **Reduced motion:** No CP-002 animations.

## 8. Data model

No Contact-specific database tables or API persistence. Booking records live in Calendly.

## 9. Acceptance-criteria mapping

| Acceptance criterion | Covered by scenario |
|----------------------|---------------------|
| AC-1: Page with navbar + footer at 1440px | GH#14 — Contact hero matches desktop and is visible on mobile |
| AC-2: Hero headline + subhead | GH#14 — Contact hero matches desktop… |
| AC-3: Calendly iframe with configured URL | GH#15 — Calendly embed loads with skeleton transition… |
| AC-4–5: Skeleton → loaded transition | GH#15 — Calendly embed loads with skeleton transition… |
| AC-6: Embed fallback when URL unset | GH#15 — Calendly embed shows fallback when URL is not configured |
| AC-7: Fallback heading + body | GH#16 — Email fallback block matches desktop… |
| AC-8: Email CTA `mailto:` | GH#16 — Email fallback block matches desktop… |
| AC-9: Mobile fluid layout | GH#14, GH#15, GH#16 mobile `Given` steps |
| AC-10: No custom calendar outside iframe | GH#15 scenarios |
| AC-11: Token lint | Pipeline gate — not a UI scenario |
| AC-12: Calendly confirmation copy | Ops / Calendly admin — Summary `5198:4102` reference |
| AC-13: Health endpoint | LP-001 BE — not duplicated |

## 10. Visual reference

- `features/CP-002/figma/reference-contacthero.png` — hero slice `5198:3440`
- `features/CP-002/figma/reference-calendlyembedslot.png` — embed slot `5186:4368` (container sizing; mock UI not implemented)
- `features/CP-002/figma/reference-emailfallback.png` — fallback slice `5198:3429`

## 11. Fidelity tolerance

- Visual-diff ratio: `0.02` (2%) for ML Travel chrome slices only
- Calendly iframe interior excluded from pixel diff

## Missing tokens

- None — all in-scope ML Travel measurements map to exact tokens per `features/CP-002/figma/notes.md`.
